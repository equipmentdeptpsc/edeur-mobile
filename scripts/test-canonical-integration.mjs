import { readFileSync } from 'node:fs';

let passed=0,failed=0;const check=(condition,label)=>{if(condition){passed++;console.log(`  PASS: ${label}`);}else{failed++;console.error(`  FAIL: ${label}`);}};
const read=(path)=>readFileSync(path,'utf8');
const environment=read('lib/canonical/environment.ts');
const client=read('lib/canonical/client.ts');
const authentication=read('lib/canonical/authentication.ts');
const auth=read('lib/canonical/authentication.ts');
const selector=read('lib/repositories/selectOperatorWorkRepository.ts');
const work=read('lib/repositories/SupabaseOperatorWorkRepository.ts');
const commands=read('lib/canonical/commandRepository.ts');
const activities=read('lib/canonical/activityMapping.ts');
const offline=read('lib/canonical/offlineEnvelope.ts');
const offlineOutbox=read('lib/canonical/offlineOutbox.ts');
const contract=read('lib/canonical/contracts.generated.ts');
const authContext=read('lib/auth.tsx');const loginScreen=read('app/login.tsx');
const packageJson=read('package.json');
const deurScreen=read('app/(tabs)/deur.tsx');const canonicalPanel=read('components/CanonicalDeurOperatorPanel.tsx');const homeScreen=read('app/(tabs)/home.tsx');

console.log('=== Canonical Mobile Integration Contract Tests ===');
check(environment.includes("mode === 'DEMO'")&&environment.includes('canonical configuration is incomplete'),'UAT configuration fails closed');
check(environment.includes("projectRef !== 'jtkctarqbwmqdcewthkn'"),'UAT project ref is pinned to isolated UAT');
check(packageJson.includes('node --env-file=.env.uat ./node_modules/expo/bin/cli start'),'UAT startup explicitly loads the nonstandard .env.uat file');
check(!auth.includes('mockRepository')&&auth.includes('signInWithPassword')&&auth.includes('/api/auth/username-login'),'canonical auth cannot use mock/PIN identities');
check(auth.includes('AUTH_SERVICE_UNAVAILABLE')&&auth.includes('INVALID_CREDENTIALS')&&auth.includes('OPERATOR_LINKAGE')&&auth.includes('SESSION_INITIALIZATION'),'canonical auth classifies failures without exposing identities or tokens');
check(loginScreen.includes("getLoginError() ?? 'Canonical sign-in failed.'")&&!loginScreen.includes('Verify credentials and UAT configuration'),'UAT login renders sanitized classified failures instead of the generic diagnostic dead end');
check(auth.includes("from('users')")&&auth.includes("from('operators')")&&auth.includes("status !== 'active'"),'authenticated user and linked active Operator are required');
check(authContext.includes("runtime.environment.mode === 'UAT'")&&authContext.includes('runtime.authentication.signIn')&&authContext.includes("runtime.environment.mode !== 'DEMO'"),'UAT app auth uses canonical session and disables PIN turnover paths');
check(work.includes('read_current_operator_deur_turnover_work')&&work.includes('turnoverStatus')&&work.includes('currentAuthorizedOperatorId'),'reliever work is projected only through the canonical turnover RPC');
check(work.includes('primaryOperatorDisplayName')&&work.includes('currentAuthorizedOperatorDisplayName')&&canonicalPanel.includes('custodyDisplayName'),'turnover custody labels use canonical display names with a safe fallback');
check(commands.includes('command_initiate_deur_turnover')&&commands.includes('command_accept_deur_turnover'),'turnover commands remain canonical RPC calls');
check(offlineOutbox.includes('expo-file-system')&&offlineOutbox.includes("'ACTIVITY_TRANSITION' | 'COMPLETE_SHIFT'")&&offlineOutbox.includes('localSequence'),'offline outbox is durable and limited to pilot-safe commands');
check(offlineOutbox.includes("Platform.OS === 'web' ? null : new File")&&offlineOutbox.includes('if (Platform.OS === \'web\') return this.readWeb();'),'web outbox never constructs Expo FileSystem File objects');
check(offlineOutbox.includes('idempotencyKey: id')&&offlineOutbox.includes('SYNC_CONFLICT')&&offlineOutbox.includes('TRANSPORT_FAILURE'),'offline replay preserves identities and stops safely on ambiguous outcomes');
check(authContext.includes('CONNECTIVITY_REQUIRED_FOR_SUBMIT')&&authContext.includes("failure('LOCAL_PENDING')"),'Submit remains online-only while queued activity and End Shift distinguish local pending state');
check(loginScreen.includes("mode === 'UAT'")&&loginScreen.includes('Username or email')&&loginScreen.includes('Demo PINs: 1234'),'login UI explicitly separates UAT credentials from demo PIN mode');
check(selector.includes("environment.mode==='DEMO'")&&selector.includes('Demo fallback is disabled'),'DEMO selects mock and UAT has no silent fallback');
check(work.includes("from('rental_equipment_lines')")&&work.includes('assignment_id')&&work.includes('rental_equipment_line_id'),'canonical Rental Line drives active work and open DEUR lookup');
check(work.includes("const open=rows.find(row=>text(row,'status')==='Draft'||text(row,'status')==='In Progress')")&&work.includes(".eq('work_date',effectiveWorkDate)"),'prior-workday open DEUR lookup is unfiltered while the separate pilot daily lookup is server-date scoped');
check(work.includes("from('deur_events')")&&work.includes(".eq('is_open',true)")&&work.includes('activeActivity'),'current activity is reconciled from the canonical event projection');
check(work.includes('for(const work of works)')&&work.includes('Canonical active activity projection is ambiguous.'),'multi-work projection hydrates and fail-closes active activity state');
check(deurScreen.includes('Redirect')&&deurScreen.includes('if (!operator) return <Redirect href="/login" />;'),'unauthenticated DEUR route fails closed to login instead of rendering blank');
check(client.includes('persistSession: true'),'UAT Supabase session persistence is enabled');
check(authentication.includes('initialSession()')&&authentication.includes('INITIAL_SESSION')&&authentication.includes('restoreSession'),'UAT Supabase session restoration resolves canonical identity');
check(authContext.includes("runtime.environment.mode === 'UAT' ? undefined")&&authContext.includes("'INITIALIZING'")&&authContext.includes('restoreOfflineContinuation')&&authContext.includes('applyCanonicalSession'),'auth initialization defers routing until online authorization or bounded offline restoration resolves');
const startBody=commands.slice(commands.indexOf(' start('),commands.indexOf(' transition('));
check(startBody.includes("'command_start_deur_shift'")&&!startBody.includes('workDate')&&!startBody.includes('deurNumber'),'Start invokes canonical command without authoritative workDate/number');
check(commands.includes("'command_submit_deur'")&&commands.includes('idempotencyKey:identity'),'Submit uses canonical command and stable caller identity');
check(commands.includes('safeShift')&&!commands.includes("'Day'")&&!commands.includes("'Night'"),'shift is optional structural metadata, not Day/Night policy');
for(const [label,value] of [['Operating','operation'],['Idle','idle'],['Standby','standby'],['Meal Break','mealBreak'],['Breakdown','breakdown']])check(activities.includes(`'${label}'`)&&activities.includes(`'${value}'`),`${label} mapping is explicit`);
check(!activities.includes("'Waiting'"),'Waiting is not a canonical UAT activity');
check(canonicalPanel.includes("label === 'Idle'")&&canonicalPanel.includes('reason')&&!canonicalPanel.includes("label === 'Standby' && idleReason"),'reason remains metadata and never selects Idle versus Standby');
check(deurScreen.includes("mode === 'UAT'")&&deurScreen.includes('CanonicalDeurOperatorPanel'),'actual UAT DEUR screen uses the canonical panel');
check(canonicalPanel.includes('startCanonicalDeur')&&canonicalPanel.includes('transitionCanonicalActivity')&&canonicalPanel.includes('endCanonicalShift')&&canonicalPanel.includes('submitCanonicalDeur'),'actual UAT controls invoke canonical commands');
check(authContext.includes('canonicalBusyRef.current')&&authContext.includes("runCanonical('start'")&&authContext.includes("runCanonical('submit'"),'Start and Submit double actions are synchronously guarded');
check(authContext.includes("commandIds.current.get('start')")===false&&authContext.includes('commandIds.current.get(key)'),'command identities are stable across retry');
check(!homeScreen.includes('Rental Line: {work.rentalLine.id}'),'normal UAT Home does not expose the raw Rental Line UUID');
check(!commands.includes('actorId')&&!commands.includes('actor_id'),'Mobile commands cannot spoof an event actor');
check(offline.includes('idempotencyKey: id')&&offline.includes('retryCount: envelope.retryCount + 1'),'offline retry preserves command identity');
check(contract.includes('Source version: 2026-08-28.phase1')&&!contract.includes('billingAmount'),'generated contract is versioned and contains no billing authority');
console.log(`=== Results: ${passed} passed, ${failed} failed ===`);if(failed)process.exitCode=1;
